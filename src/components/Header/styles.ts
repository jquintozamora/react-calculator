import styled from "styled-components";

import { eeBlue, eeLightBlue } from "../../styles/colors";

export const StyledHeader = styled.div`
  background-color: ${eeBlue};
  padding: 20px 0 20px 25px;
  border-bottom: 1px solid ${eeLightBlue};
  min-width: 250px;
`;
